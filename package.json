{
  "name": "spavix",
  "version": "1.0.0",
  "description": "AI-powered room transformation tool",
  "scripts": {
    "dev": "dotenv -e .env concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend && npm run start --prefix frontend"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "concurrently": "^8.2.2",
    "dotenv-cli": "^7.4.4"
  },
  "dependencies": {
    "dompurify": "^3.3.1",
    "next-themes": "^0.4.6",
    "react-easy-crop": "^5.5.6",
    "zustand": "^5.0.9"
  }
}
